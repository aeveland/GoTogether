{
  "name": "gotogether-railway",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node simple-server.js",
    "dev": "node server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "prisma": "^5.3.1"
  }
}
